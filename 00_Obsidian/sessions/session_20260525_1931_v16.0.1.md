# 현자의 거울 스튜디오 세션 기록 — v16.0.1
- 일시: 2026-05-25 19:31
- 버전: v16.0.1 (RAG Memory Optimization Layer)

## 📌 변경 사항 요약
1. **References Memory 최적화 함수 추가**:
   - `app_v16_0_1.py` 에 최신 References를 압축 가공하는 `build_condensed_reference_context()` 추가.
   - 최근 8개 파일은 상세 표시(요약, 키워드, 위키링크, 원본 SOURCE 및 본문 최대 800자)하고 9번째 파일부터는 본문을 생략해 토큰을 절약합니다.
   - 파일 간의 중복된 위키링크들을 추출하여 최상단에 중복 제거된 통합 **전역 RAG 위키링크 모음**으로 정돈 제공합니다.
2. **RAG 파일 안전 필터링 도입 (추가 안전 조건)**:
   - 본문 300자 미만 제외
   - 특정 오염 문자열(span, display, background-color, 자동저장:, [EP001]) 포함 파일 제외
   - SOURCE/키워드/위키링크가 모두 비어 있는 파일 제외
   - 동일 파일명 중복 발생 시 수정 시간(`mtime`) 기준 최신 파일만 채택
   - 제외된 파일이 발생하면 `st.caption` 제외 로그로 실시간 표시
3. **임포트 경로 및 구동 배치 파일 갱신**:
   - `sage_popups.py` 내의 `save_workspace_state` 및 RAG 임포트 타깃을 `app_v16_0_1`로 전면 갱신.
   - `RUN_APP.bat`, `RUN_DEBUG.bat`, `RUN_APP.vbs` 의 구동 타깃을 `app_v16_0_1.py` 로 변경 (포트 8505).
   - RAG 로딩 성공 시 캡션 문구를 `"🧠 References Memory Loaded: X files"` 로 개선하고 `try-except`로 대화 안정망을 보장.

## 🛠️ 수정된 함수 목록
- `app_v16_0_1.py` ➔ `build_condensed_reference_context(memory_items, max_chars=30000)` (신규)
- `sage_popups.py` ➔ `popup_assistant()` (임포트 갱신, RAG 연동 부분 업데이트)

## 📅 다음 작업 예정
- Part 5 Image 렌더링 검토 및 Part 6 Opal 신규 구현 작업 대기
