# Session Log - v15.9.32 (2026-05-24 11:00)

## 변경 사항 요약
- **글로벌 모델 스위칭 시스템 도입**:
  - 좌측 사이드바에 글로벌 모델 선택기(`gemma4:e2b`, `gemma4:e4b`)를 추가하여 기본적으로 전체 파트에 일괄 자동 적용되도록 구현.
  - 각 파트 화면의 최상단 우측에 있는 개별 모델 선택기는 필요에 따라 개별적으로 변경해 사용할 수 있도록 세션 독립화(프록시 모델 바인딩 구조).
  - 전체 파트 렌더링 전후에 selectbox 세션 값을 동적으로 가로채어 개별 파트 키(`p1_selected_model` ~ `p8_selected_model`)에 자동 갱신 및 저장되도록 하여 [절대 수정 금지] 구역인 `render_part1()`, `render_part2()`, `render_part34()` 내부 수정을 우회하고 무결성 유지.
- **Ollama 실시간 모델 상태 표시 동적화**:
  - `sage_engine.py` 내 `check_ollama_status` 함수가 `target_model` 파라미터를 받아 동적 검사를 진행할 수 있도록 변경.
  - 사이드바 내의 Ollama 연결 상태 표시 컴포넌트(`[OK] Ollama | ...`)가 하드코딩되었던 부분(`OLLAMA_MODEL` 고정)을 제거하고, 현재 선택된 글로벌 모델을 동적으로 검사하여 화면에 실시간 표시하도록 수정.
- **구동/실행 스크립트 실행 타겟 일괄 갱신**:
  - `RUN_APP.bat`, `RUN_DEBUG.bat`, `RUN_APP.vbs` 파일의 Streamlit 실행 대상을 신버전 `app_v15_9_32.py`로 일괄 업데이트.

## 수정/추가된 함수 목록
- `sync_global_model_to_parts(model)` [NEW] : 글로벌 모델 선택 시 파트별 모델 키 및 UI 컴포넌트 세션 상태 덮어쓰기/동기화 헬퍼 함수
- `check_ollama_status(target_model)` [MODIFY] (in `sage_engine.py`) : Ollama 모델 확인 시 특정 모델을 타겟팅하여 동적으로 체크 가능하도록 매개변수 및 로직 개선
- `init_session_state()` [MODIFY] : 디폴트 값 딕셔너리에 글로벌 모델 및 개별 파트 모델 키 초기값 탑재
- `save_workspace_state()` [MODIFY] : `keys_to_save` 리스트에 신규 모델 상태 키 등록
- `st.sidebar` 렌더링 영역 [MODIFY] : 글로벌 모델 선택 Selectbox 및 실시간 동기화 처리 로직 이식, Ollama 상태 확인 동적화 적용
- 하단 파트 라우팅 블록 [MODIFY] : 파트 1 ~ 8에 대한 개별 모델 프록시 바인딩 및 변경 감지 로직 적용

## 다음 작업 예정
- 글로벌 스위칭 시스템 운영 안정성 모니터링 및 실 피드백 반영
- Ollama Gemma 모델 스위칭 성능 및 RAG 검수 결과 퀄리티 정량 분석
