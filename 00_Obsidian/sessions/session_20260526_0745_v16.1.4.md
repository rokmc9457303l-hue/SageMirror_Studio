# Sage Mirror Studio Session — v16.1.4

- 일시: 2026-05-26 07:45
- 버전: v16.1.4

## 변경 사항 요약
- **Memory Layer 안정화 및 분리**: app_v16_1_3.py 내부에 있던 RAG, References, Workspace, Session 상태 관리 비-UI 로직을 신규 생성된 순수 Python 모듈 `memory_state_manager.py`로 분리 및 격리하였습니다.
- **의존성 배제**: `memory_state_manager.py`에서 Streamlit 임포트 및 세션 상태 접근을 배제하고 순수한 Python 함수로만 재구성하여 UI 독립적인 상태를 확보했습니다.
- **래핑 구조 연동**: `app_v16_1_4.py`를 새로 생성하고 분리된 모듈의 함수들을 import하여 UI 토스트, 에러 핸들링, 세션 상태 동기화 및 Recent Activity Sync 기능이 온전하게 동작하도록 래퍼를 구성하여 결합했습니다.
- **구동 파일 타겟 버전 갱신**: `RUN_APP.bat`, `RUN_DEBUG.bat`, `RUN_APP.vbs` 파일들의 타겟 스크립트를 `app_v16_1_4.py`로 갱신하여 릴리즈를 일원화했습니다.

## 수정/추가된 함수 목록
- `memory_state_manager.py`:
  - `clean_prompt_contamination(text)`
  - `sanitize_workspace_prompt_values_once_core(workspace_file_path)`
  - `save_workspace_state_core(state_dict, workspace_file, secrets_file)`
  - `load_workspace_state_core(workspace_file, secrets_file)`
  - `extract_text_from_uploaded_file(uploaded_file)`
  - `convert_text_to_markdown_structure(raw_text, source_name)`
  - `build_all_parts_common_tags_preview(detection, part_rag_tag_map, unique_keep_order_fn)`
  - `save_reference_markdown_file(markdown_text, preview_data, source_name, ref_dir)`
  - `parse_markdown_reference(content)`
- `app_v16_1_4.py`:
  - `save_workspace_state()` (래핑 교체)
  - `load_workspace_state()` (래핑 교체)
  - `sanitize_workspace_prompt_values_once()` (래핑 교체)
  - `extract_text_from_uploaded_file(uploaded_file)` (래핑 교체)
  - `convert_text_to_markdown_structure(raw_text, source_name)` (래핑 교체)
  - `build_all_parts_common_tags_preview(detection)` (래핑 교체)
  - `save_reference_markdown_file(markdown_text, preview_data, source_name)` (래핑 교체)

## 다음 작업 예정
- Gemma Assistant 및 RAG 태그 시스템, 메모리 관리 레이어의 분리가 완료되었으므로, 실제 UI 작동 상태 테스트 진행.
- 다음 단계인 Part 5/6/7/8 기능 점검 및 추가 리팩토링 진행.
